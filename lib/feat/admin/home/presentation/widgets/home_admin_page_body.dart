import 'package:flutter/widgets.dart';

class HomeAdminPageBody extends StatelessWidget {
  const HomeAdminPageBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center( 

      child: Column(
        children: [
          Text("Home Admin Page")
        ],
      ) 
    );
  }
}