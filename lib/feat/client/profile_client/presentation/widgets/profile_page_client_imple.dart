import 'package:flutter/material.dart';

class ProfilePageClientBody extends StatelessWidget {
  const ProfilePageClientBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}