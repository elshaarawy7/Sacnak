import 'package:flutter/material.dart';

class ChatePageClientBody extends StatelessWidget {
  const ChatePageClientBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        
      ],
    );
  }
}